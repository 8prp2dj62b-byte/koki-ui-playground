name: Kleinanzeigen Bridge Manual

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  bridge:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Manual bridge only
        run: echo 'Use Kleinanzeigen Bridge Trigger for normal connections.'
